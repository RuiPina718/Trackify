export const ADMIN_EMAIL = 'ruialexandrepina@gmail.com';

/** Base URL for internal API calls (calendar sync, etc.). Configurable per environment. */
export const API_BASE_URL = import.meta.env.VITE_API_URL ?? '';
